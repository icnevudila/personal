// Signature component for portfolio pages
class Signature {
  constructor() {
    this.init()
  }

  init() {
    // Add signature functionality here if needed
    console.log('Signature component loaded')
  }
}

// Initialize when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
  new Signature()
})
